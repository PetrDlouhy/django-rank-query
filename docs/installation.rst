============
Installation
============

At the command line::

    $ easy_install django-rank-query

Or, if you have virtualenvwrapper installed::

    $ mkvirtualenv django-rank-query
    $ pip install django-rank-query
